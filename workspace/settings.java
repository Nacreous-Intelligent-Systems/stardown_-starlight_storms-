package workspace;
public class settings {
    // This is the settings file.

    // ~~TEXT SCROLL SPEED~~
    // How fast the text scrolls. Lower is faster.
    // Supports an integer from 0 to 5. (0 prints the entire screen instantly.)
    // Default: 2 (50ms per character)
    // Fast: 1 (25ms per character)
    // Slow: 4 (100ms per character)
    public static int textScrollSpeed = 2;
}
